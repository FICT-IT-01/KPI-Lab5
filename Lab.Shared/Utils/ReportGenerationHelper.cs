namespace Lab.Utils.Utils
{
    public class ReportGenerationHelper
    {
        /// <summary>
        /// Generate a pool of reports
        /// </summary>
        /// <param name="filesAmount"> Amount of file which should be generated </param>
        public void GenerateLogFiles(int filesAmount)
        {
            // Create file writer
            // Create a cycle and fill file using a method of StreamWriter.WriteLine();
            // Save the file
        }
    }
}